import type{Metadata}from'next';import{notFound}from'next/navigation';import Portal from'../../components/Portal';import{getOrtho}from'../../lib/orthos';export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const ortho=await getOrtho((await params).slug);if(!ortho)return{title:'Map not found — Rasuwa Flood Impact Data Portal',openGraph:{images:[]},twitter:{images:[]}};const description='Explore drone imagery and mapped flood-impact information for Rasuwa.';return{title:'Rasuwa Flood Impact Data Portal',description,openGraph:{title:'Rasuwa Flood Impact Data Portal',description,images:[]},twitter:{card:'summary',title:'Rasuwa Flood Impact Data Portal',description,images:[]}}}export default async function MapPage({params}:{params:Promise<{slug:string}>}){const ortho=await getOrtho((await params).slug);if(!ortho)notFound();return <Portal orthos={[ortho]} initialSlug={ortho.slug} publicView/>}
