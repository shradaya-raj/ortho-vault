'use client';
import {useEffect,useRef} from 'react';import type {Ortho} from '../lib/orthos';
export default function MapView({ortho,showOrtho,showBoundary}:{ortho:Ortho;showOrtho:boolean;showBoundary:boolean}){const element=useRef<HTMLDivElement>(null);useEffect(()=>{if(!element.current)return;let disposed=false;let map:import('leaflet').Map|undefined;import('leaflet').then(L=>{if(disposed||!element.current)return;const bounds=L.latLngBounds([ortho.south,ortho.west],[ortho.north,ortho.east]);map=L.map(element.current,{zoomControl:false}).fitBounds(bounds,{padding:[28,28]});L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{attribution:'© OpenStreetMap contributors',maxZoom:20}).addTo(map);if(showOrtho&&ortho.image_url)L.imageOverlay(ortho.image_url,bounds,{opacity:.9}).addTo(map);if(showBoundary)L.rectangle(bounds,{color:'#e1a83f',weight:2,fill:false}).addTo(map).bindTooltip(ortho.name,{permanent:true,direction:'center',className:'ortho-label'});L.control.zoom({position:'topleft'}).addTo(map)});return()=>{disposed=true;map?.remove()}},[ortho,showOrtho,showBoundary]);return <div ref={element} className="leaflet-map" aria-label={`Interactive map of ${ortho.name}`}/>}
